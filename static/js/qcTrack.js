
$(document).ready(function(){
  /* setup - for qcTable
  $('#qcTable thead tr').clone(true).appendTo( '#qcTable thead' );
  $('#qcTable thead tr:eq(1) th').each( function (i) {
      var title = $(this).text();
      $(this).html( '<input type="text" placeholder="Search '+title+'" />' );

      $( 'input', this ).on( 'keyup change', function () {
          if ( tableqc.column().search() !== this.value ) {
              tableqc
                  .column(i)
                  .search( this.value )
                  .draw();
          }
      } );
  } );


  var tableqc = $('#qcTable').DataTable( {

      "scrollY": 400,
      "scrollX": true,
      scrollCollapse: true,
        autoWidth:         true,
         paging:         true,
      columnDefs: [
        { "width": "40px", "targets": [4] }
      ]
  } );

*/
});
